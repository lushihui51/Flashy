# 2026-09-24 — Sync mid task 015

**Date:** 2026-09-24. **Prompted by:** a `/sync` pass after task 015 T1–T4 landed in the working tree (uncommitted) and the planning session added ADRs 055–056. **Outcome:** diagnosis and doc corrections only, no code changes; all five proposed fixes confirmed and applied to the working tree (uncommitted).

## What was checked

Every task file (001–015), every ADR (001–056), AGENTS.md, and README.md against the working tree at `700b3c8` plus the uncommitted T1–T4 work, with each finding verified directly rather than from memory. Mechanical markers are clean: six unchecked boxes, all in task 015 (T5–T10) and all genuinely unbuilt (no new `db_*` name from the Contracts exists, `tests/api_tests/test_layering_guard.py` does not exist, every session-call site ADR 055 lists is still present); no `[BLOCKED]` marker (the four grep hits are instructional text in tasks 013, 014, 015); no pending-justify section; the only D-number references are task 003's, kept resolvable there by design. Task 015 T1–T4's Done-when criteria all hold: 312 backend tests, 419 frontend tests, and every grep in those four criteria re-run this session.

The ADR supersession chain is reciprocal. ADR 034's Status carries the ADR 055 amendment (uncommitted); ADR 055's Context matches the code's current violations exactly; ADR 056 is implemented by T1–T2. Task files citing ADRs 010, 015, 029, 030 (tasks 001, 003, 006) already note their supersession.

T10's naming rule (`stage_` in a `db_*` name if and only if the body never commits, read verbs exempt) was pre-run with an `ast` scan over the current `app/database_ops/`: every function already satisfies it, so T10 has no hidden blocker from names.

## Fixed in this pass (all five confirmed 2026-09-24)

1. **Task 009 — new "Superseded since (sync 2026-09-24)" section** (the file has none). Task 015 T4 renamed the three practice-run writers this file's rename map and T-notes name (`db_create_practice_run/deck/card` → `db_stage_create_practice_*`, ADR 055); `_POSITION_CONSTRAINT` still lives in `app/services/practice_run.py:75` until task 015 T7 moves it to `app/database_ops/practice_card.py` as `POSITION_CONSTRAINT`.
2. **Task 010 — one Superseded bullet.** `db_append_mastery_log` and `db_clear_mastery` (contract lines 58, 60 and two T-details mentions) are `db_stage_append_mastery_log` and `db_stage_clear_mastery` (ADR 055, task 015 T4). The card lock is still an inline `db.execute(text(...))` in `app/services/mastery.py:54,112` until task 015 T7 puts it behind `db_lock_card`.
3. **Task 013 — one Superseded bullet.** Twenty-one mentions across MD-5, the `apply_deletion` order, and the `database_ops` contract: `db_scrub_shown_prompt_ids`, `db_clear_mastery_for_deck`, `db_append_mastery_log`, and the six plural `db_delete_*` carry `stage_` (ADR 055, task 015 T4). `delete_deck`/`delete_card` still take `(db, strategy, row)` and look the parent up with `db.get` (`app/services/deletion.py:171,180-182`) until task 015 T6.
4. **Task 015 intro — branch name.** Line 3 says `feat/persistence-surface`; the branch is `feat/persistence-surface-and-empty-practice`. Same line says "cut from `main` after `docs/sync-2026-09-23` merges": `git log main..HEAD` shows `700b3c8`, so the sync branch has not merged and this branch carries its commit. Applied: the name is corrected and the clause added; the merge order itself is a process note for the user, not a doc fix.
5. **Task 015 Contracts — one line citation drifted.** `practice_run.py:828-843` (the savepoint block that becomes `db_try_stage_create_practice_card`) is `:852-866` after T1 added lines above it. Inside the Contracts section, so it was flagged rather than assumed; confirmed and applied, keeping the `700b3c8` number alongside for the T7 builder. `mastery.py:55`/`:113`, `deck_practice_config.py:109-113`, `deck.py:110`, `card.py:134`, `practice_deck.py:9`, and `test_practice_run.py:598` all still point where they claim.

## Deferred (unchanged from 2026-09-23)

- Run-start ordering "unseen cards first, then ascending mastery" (`app/services/practice_run.py`, `_snapshot_and_generate_deck`) and bearer-token injection through `window.Clerk.session.getToken()` (`frontend/src/api/client.ts`) are still recorded in no ADR. The previous sync left them unrecorded by decision; nothing since has changed that.

## In-flight, not drift

The code contradicts ADR 055 at exactly the sites task 015 T5–T9 plan to move: `app/dependencies.py:50,74,81,87`; `db.get` in `app/routers/api/field_def.py:98,118,137`, `app/routers/api/card.py:113`, `app/services/deletion.py:171,180,182`, `app/services/deck_batch_edit.py:222,259`; `db.add` in `activity.py`, `deck_copy.py`, `deck_create.py`, `deck_batch_edit.py`; statements in `mastery.py`, `deck_practice_config.py`, `deck_batch_edit.py`, `practice_run.py`; the upward import at `app/database_ops/subject.py:10`. Every one is named in a T5–T9 Details line. Nothing to fix here.

## Flagged for /distill (not edited here)

- **AGENTS.md, carried forward from 2026-09-23, all still present verbatim:** "one module per table plus `practice_generation.py`" (10 modules for 12 tables; T5 adds `app_user`, leaving `card_field_value`); "at least one prompt and one answer, enforced on create, batch edit, and archive" (a configuration rule, not a deck rule); "the archive endpoints stay in the code, unexposed" (live routes with no UI caller); `<router>_payloads.py` vs `deletion_payloads.py`.
- **AGENTS.md, new, timed after task 015 T10:** the Conventions bullet on backend layering cites ADR 034 only and says "public functions `db_*`-prefixed". Once the layering guard lands it should carry ADR 055's rules in one sentence: outside `database_ops`, no statement construction and no `Session` method beyond `commit`/`rollback`/`flush`/`refresh`; a non-committing writer is `db_stage_*`, a savepoint-guarded one `db_try_stage_*`.
- **README, carried forward from 2026-09-23, all still present verbatim:** the module-per-table claim (now also understating ADR 055: the layer owns every session call, not only "the SQL"); "the confirmation shows the same plan as counts"; "weighted by 100 minus the field's mastery" (floored at 1, retry draw reduced by forced fields); the badge list omits Abandoned; "Every API endpoint verifies the Clerk session token" (root `GET /` does not); "a practice in progress is deleted only when a deck or field it needs is deleted".
- **README, new, optional:** "How it works" says a practice "generates a queue of cards"; since ADR 056 a practice that would generate none is refused. One clause would record it.

Every other concrete README claim holds: EMA prior 50 and scores 0/33/67/100 (`app/mastery/ema.py:13,20`), the five `.env` variables (`app/config.py:5-12`), Postgres 16 in CI, the `/api` proxy, the ERD file, per-test schema create/drop in `tests/conftest.py:33,35`, text-only field creation (`frontend/src/lib/fieldTypes.ts`).

## Working-tree note

T1–T4 are complete and uncommitted, alongside the planning session's ADR 034 amendment, ADRs 055–056, and the task 015 file. Several sessions share this checkout, so staging is by hunk (`git add -p`); nothing in this pass changes that.
