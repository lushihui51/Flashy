# 2026-09-24 — Final sync before the UI/UX cycle

**Date:** 2026-09-24 (second pass this date; the first is `2026-09-24-sync-mid-task-015.md`). **Prompted by:** a `/sync` pass requested as the last consistency check before work moves from core functionality to UI/UX. **Outcome:** docs, two ADR Status notes, and two code comments reconciled in the working tree (uncommitted); no logic changed; all ten proposed fixes confirmed and applied 2026-09-24; nothing deferred to a planning session.

## What was checked

Every task file (001–016), every ADR (001–057), AGENTS.md, and README.md against `main` at `78fd579` (clean tree). The previous two syncs verified everything against `700b3c8` plus task 015 T1–T4, so this pass re-verified in full what changed since — task 015 T5–T10, task 016 T1–T2, and two rollback tests, 28 files all under `app/` and `tests/` — and re-ran every earlier Done-when criterion whose grep or named test targets one of those files. Two read-only audits ran in parallel (AGENTS.md and README claim by claim; ADR bodies, task Done-when criteria, and Contracts against the changed files) and every finding below was re-read directly.

Mechanical markers are clean: zero unchecked boxes; the four `[BLOCKED]` hits are instructional text (tasks 013, 014, 015); no pending-justify section; the only D-number references are task 003's, kept resolvable there by design. Task 015 T4–T10 and task 016 T1–T2: every Done-when grep re-run and holding (`app/dependencies.py` issues no statement and `app_user.py` has three `db_*`; no `db.get` in routers or the deletion service; the three T7 services build no statement; `deck_create.py`/`deck_copy.py`/`deck_batch_edit.py` call no `db.add`/`db.exec`/`db.get`; `db_create_field_def` absent from `app/` and `tests/`; `field_def_create.py` calls only `commit`/`rollback`/`refresh`; the create handler no longer touches; `is duplicated` appears exactly twice in `deck_batch_edit.py`; none of the 17 pre-rename names survives). Earlier Done-when greps re-run and holding: task 003 L808 (the two density tests), 009 L95 (`practice_session` absent), 010 L109, 011 L36, 012 L45/L54, 013 L175/L202/L211/L220, 014 L53.

ADR 055's four rules were verified against `tests/api_tests/test_layering_guard.py` and the code: outside `database_ops`, only `from sqlmodel import Session` and `sqlalchemy.exc` are imported and only `commit`/`rollback`/`flush`/`refresh` are called; no upward import; all 93 `db_*` functions satisfy the naming rule (15 unprefixed committing writers, every `db_stage_*`/`db_try_stage_*` non-committing, every `read`/`fetch`/`count`/`next`/`lock` unprefixed and non-committing). The IMMEDIATE-on-success asymmetry ADR 055 records is documented at `app/database_ops/practice_card.py:35-38`. ADR 057's service body matches its Decision (`app/services/field_def_create.py:11-27`). The supersession chain is reciprocal (ADR 034 ↔ 055; ADR 057 amends nothing). Tests: 323 backend (316 after task 015, plus the two rollback tests and task 016's five), 419 frontend; both green this session.

## Fixed in this pass (all ten confirmed 2026-09-24)

Each item names what the doc or comment said before the edit; the edit replaced or appended the current truth in place, labelled "sync 2026-09-24, final pass" where a bullet needed a label. After the edits: the task 015 T7 grep over `app/services/practice_run.py` still returns nothing, no edited code line exceeds 100 characters, and the layering guard plus `test_no_mastery_arithmetic_outside_strategy` (which scans `mastery_log.py`) pass. The Markdown formatter hook that runs after edits also normalized task 015's Naming table separator and inserted blank lines between the Contracts paragraphs: whitespace only, outside the inserted section, worth knowing when staging by hunk.

1. **Task 009 — Superseded bullet.** Its last sentence says `_POSITION_CONSTRAINT` "still lives in `app/services/practice_run.py` until task 015 T7". It is `POSITION_CONSTRAINT` at `app/database_ops/practice_card.py:16`, and the savepoint it guards is `db_try_stage_create_practice_card` (`:27-45`); the rename map's "(the `_POSITION_CONSTRAINT` string constant must follow)" reads accordingly.
2. **Task 010 — Superseded bullet.** Its last sentence says the card lock "is still an inline `db.execute(text(...))` ... until task 015 T7". It is `db_lock_card` (`app/database_ops/mastery_log.py:19-23`), called at `app/services/mastery.py:54` and `:110`; the contract's `db_log_review_group` is `db_stage_log_review_group` (`app/database_ops/review_log.py:64`); rebuild attribution is `db_read_run_ids_for_practice_cards` (`app/database_ops/practice_card.py:48-61`).
3. **Task 013 — Superseded bullet.** Its last sentence says `delete_deck`/`delete_card` "still take `(db, strategy, row)` ... until task 015 T6". They take `(db, strategy, user_id, row)` (`app/services/deletion.py:175-194`), read the parent through `db_read_subject`/`db_read_deck`, and `delete_card` no longer reads the subject; the "Executing the closure" Callers line (L91) and the order contract's `db_log_review_group` (L60) read accordingly; the API section's 422 list (L131) gained task 016 MD-1's `cards.update id {id} is duplicated`.
4. **Task 003 — new Superseded bullet.** D10 (L53) and §2.3's "§2.2 only covers cards created atomically" (L171) list two density paths; ADR 057 (task 016 T1) added the standalone `POST /decks/{deck_id}/fields` through `app/services/field_def_create.py:17-21`.
5. **Task 015 — new Superseded section.** The Contracts line "`db_create_field_def` is unchanged" (task 016 T1 deleted it); T9's Notes wording slips task 016's intro names ("would now add a duplicate row": the primary key rejects it and the effect was a misleading 422; "consults the identity map": the old tolerance was `db.get`'s SELECT fallback on an unreferenced flushed row), with the precondition now enforced by task 016 T2; the intro's "`docs/sync-2026-09-23` has not merged" (merged as PR #28 before this branch's PR #29).
6. **Task 016 — MD-3 as built.** `feat/dense-field-create` was never cut: T1 and T2 are `5abae3c` and `633853a` on `feat/persistence-surface-and-empty-practice`, merged with it in PR #29 (`git branch --contains 5abae3c` lists no such branch).
7. **ADR 054 — Status note.** Its Consequences say "a source-scan guard test is needed"; task 015 T3 (MD-1) built it: `test_alembic_env_imports_app_database_before_reading_metadata` and `test_metadata_naming_convention_is_the_adr_054_templates` in `tests/api_tests/test_schema_guard.py`.
8. **ADR 034 — Status note (optional).** Its Context says every `database_ops` read takes `user_id`; reads keyed by an already ownership-checked parent (the closure's `*_for_decks`/`*_for_subjects` reads, `db_read_card_for_deck` at `app/database_ops/card.py:56-67`) and the `app_user` module carry none by design, and the copy helpers are ADR 014's exception; the Costs figure "57 `db_*` functions" is 93.
9. **Comment, `app/database_ops/mastery_log.py:31`.** "apply_rating's caller takes a per-card advisory lock instead" — `apply_rating` takes it itself (`app/services/mastery.py:54`, `db_lock_card`); so does `record_review_group` before calling it. Text-only.
10. **Comment, `app/services/practice_run.py:837-839` (optional).** The requeue comment describes SET CONSTRAINTS and the savepoint without naming `db_try_stage_create_practice_card`, which now issues them; task 015 T7 kept it deliberately, so this is a one-clause pointer, not a correction.

## Not drift

- "Session" for a run survives in backend docstrings, comments, and local names (`app/services/practice_run.py`, `app/database_ops/practice_run.py`, `app/models/practice_*.py`) and in frontend identifiers; ADR 038's Decision scoped the rename to task 009's identifier map and its Status records the frontend leftovers. Not proposed.
- `DELETE /fields/{id}/hard` calls `db_hard_delete_field_def` directly, outside the closure: the exception ADR 049 keeps and AGENTS.md names (as "the unexposed archive endpoints").
- Reads without `user_id` (`db_read_card_for_deck`, `app_user`, the closure's parent-keyed reads) are recorded in task 013's and task 015's Contracts; item 8 above offers the ADR-level note.

## Deferred (unchanged from 2026-09-23 and 2026-09-24)

Run-start ordering "unseen cards first, then ascending mastery" (`app/services/practice_run.py`, `_snapshot_and_generate_deck`) and bearer-token injection through `window.Clerk.session.getToken()` (`frontend/src/api/client.ts`) remain recorded in no ADR, by the user's decision on 2026-09-23. Nothing else found here needs a planning session.

## Flagged for /distill (not edited here)

- **AGENTS.md, carried forward and re-verified present:** "one module per table plus `practice_generation.py`" (11 for 12; `card_field_value`'s writes live in `card.py`); "at least one prompt and one answer, enforced on create, batch edit, and archive" under `deck` (`deck_create.py:38-39`, `deck_batch_edit.py:138-139`, `field_def.py:113-115` enforce only ≥2 fields; prompt-and-answer is `deck_practice_config.py:179-182`); "the archive endpoints stay in the code, unexposed" (live routes at `field_def.py:105` and `:122`, no UI caller; the second is a hard delete); `<router>_payloads.py` vs `deletion_payloads.py`.
- **AGENTS.md, now live (the 2026-09-24 pass timed it after T10):** the Conventions bullet on backend layering cites ADR 034 only and says "public functions `db_*`-prefixed"; ADR 055 and `test_layering_guard.py` are in force: outside `database_ops`, no statement construction and no `Session` method beyond `commit`/`rollback`/`flush`/`refresh`; layers depend downward only; a non-committing writer is `db_stage_*`, a savepoint-guarded one `db_try_stage_*`.
- **AGENTS.md, new:** the `card_field_value` entry's "adding a field backfills every existing card in the same transaction" now holds on every path and has an ADR to cite (ADR 057).
- **README, carried forward and re-verified present:** the module-per-table claim (L45; also understates ADR 055); "the confirmation shows the same plan as counts" (L41; `CardStandaloneForm.tsx:314-318` is static); "weighted by 100 minus the field's mastery" (L35; floored at 1, `practice_generation.py:41`; a retry's draw count is reduced by the forced fields, `:83`); the badge list omits Abandoned (L25; `RunBreakdown.tsx:19-24`); "Every API endpoint verifies the Clerk session token" (L45; `app/main.py:32-34` root `GET /`); "a practice in progress is deleted ... only when a deck or field it needs is deleted" (L33; a completed run whose decks all go is deleted too, and an active multi-deck run losing one deck survives, `deletion.py:120-122`, `database_ops/practice_run.py:134-150`); "generates a queue of cards" (L23; refused with `no_cards` since ADR 056).

Every other concrete AGENTS.md and README claim holds: the bypass needs both variables (`dependencies.py:61-62`), the one status transition (`practice_run.py:389`), all 38 handlers take `CurrentUserDep`, the array operators, the ESLint date and react-refresh rules, the `src/` alias in both configs, the Vitest environment and `afterEach(cleanup)`, `EMA_BETA`, harshest-wins in both places, the bounded delta lookup, `X-Timezone` on every request, the five `.env` variables, `azp` against `permitted_origins`, per-test schema create/drop, Postgres 16 and Node LTS in CI, text-only field creation, the unrouted copy service, React 19, Tailwind 4, Radix popover and dialog, the ERD file.
