# 015 — Sole persistence surface and the empty practice

The hardening cycle from the 2026-09-23 /plan session, following the final sync before dogfooding (`docs/cc/2026-09-23-final-sync-before-dogfooding.md`): a practice can no longer be created with zero cards, `database_ops` becomes the only code that touches the database, and two guards close the gaps the sync found. Branch: `feat/persistence-surface-and-empty-practice` (it needs ADRs 053–054 and this file present; sync 2026-09-24: `docs/sync-2026-09-23` has not merged to `main`, so this branch carries its commit `700b3c8` ahead of `main`).

Earlier task files affected by name only, for the next sync's Superseded bullets (nothing here invalidates a checked task): task 009's rename map (`_POSITION_CONSTRAINT` moves out of `practice_run.py`; `db_create_practice_*` become `db_stage_create_practice_*`), task 010's write-path contract (`db_append_mastery_log` becomes `db_stage_append_mastery_log`; the card lock is taken through `db_lock_card`), task 013's contracts (`db_scrub_shown_prompt_ids`, the plural `db_delete_*`, `db_clear_mastery_for_deck` gain the `stage_` prefix; `delete_deck` and `delete_card` gain `user_id`). Task 004's carried invariant 2 is restored by T1, which edits its Superseded bullet.

## ADRs

Decisions this file implements; full context and rejected alternatives live in the ADRs.

- **ADR 055 — `database_ops` is the sole persistence surface**: outside it, no statement construction and no `Session` method other than `commit`, `rollback`, `flush`, `refresh`; layers depend downward only, with the subject router calling `touch` before `db_update_subject`; the requeue savepoint is `db_try_stage_create_practice_card`; a non-committing writer is `db_stage_<verb>_<noun>`; one source-scan guard enforces all four rules. Amends ADR 034.
- **ADR 056 — A practice that generates no cards is refused**: start and rerun refuse with `no_cards` when zero practice cards were written across every selected deck, checked after generation and before commit; the frontend owns the message, keyed on the code.

## Minor decisions

- **MD-1**: Two source-scan tests in `test_schema_guard.py` enforce ADR 054: `alembic/env.py` imports `app.database` before reading `SQLModel.metadata` (checked on `ast` nodes, so a commented-out import fails), and the five naming templates hold after the import. Rejected: no guard, which ADR 054 itself names as needed.
- **MD-2**: The `no_cards` failure renders inline in the create page's existing top slot above the configuration list, and in the details page's existing rerun error line, as the sentence `NO_CARDS_MESSAGE` owned by the frontend and keyed on the code; ADR 035 is unchanged. Rejected: the bottom slot under the name field (off-screen on a long list, backend-owned copy); a page-owned single-action dialog, which needs an ADR 035 amendment for a failure the user can fix in place.

## Contracts

### Error codes (ADR 056)

`POST /api/practice_runs` returns 400 with body `{"detail": {"code": "no_cards", "message": "generation produced no practice cards across the selected decks", "config_id": null}}` when, after every selected configuration was validated and snapshotted, no `practice_card` row was written. The check is `next_position == 0` after the loop in `start_practice_run`, raised before `db.commit()`; the session is discarded without commit, exactly as the existing `stale_config` raise after `db_stage_create_practice_run`. `POST /api/practice_runs/{id}/rerun` returns 400 with `{"detail": {"code": "no_cards", "message": "generation produced no practice cards across the surviving decks"}}` under the same check, after the `nothing_to_rerun` check. `RunStartError`'s and `RerunError`'s docstrings, `start_practice_run`'s and `rerun_practice_run`'s docstrings, and the rerun route's docstring list the code.

### Frontend copy (MD-2)

`frontend/src/lib/practiceCopy.ts` exports `export const NO_CARDS_MESSAGE = 'This practice would have no cards. Either the selected decks have no cards, or every card is blank in every field its configuration shows on one side.';`. The create page's `onError` renders it through `setTopError` when `error instanceof ApiDetailError && error.detail.code === 'no_cards'`; the details page's rerun `onError` renders it through `setRerunError` under the same test, and `error.message` otherwise, as today.

### Naming (ADR 055)

| today | after T4 |
|---|---|
| `db_create_practice_run` | `db_stage_create_practice_run` |
| `db_create_practice_deck` | `db_stage_create_practice_deck` |
| `db_create_practice_card` | `db_stage_create_practice_card` |
| `db_insert_review_logs` | `db_stage_create_review_logs` |
| `db_log_review_group` | `db_stage_log_review_group` |
| `db_append_mastery_log` | `db_stage_append_mastery_log` |
| `db_update_practice_card_status` | `db_stage_update_practice_card_status` |
| `db_renumber_pending_practice_cards` | `db_stage_renumber_pending_practice_cards` |
| `db_scrub_shown_prompt_ids` | `db_stage_scrub_shown_prompt_ids` |
| `db_clear_mastery` | `db_stage_clear_mastery` |
| `db_clear_mastery_for_deck` | `db_stage_clear_mastery_for_deck` |
| `db_delete_subjects` | `db_stage_delete_subjects` |
| `db_delete_decks` | `db_stage_delete_decks` |
| `db_delete_field_defs` | `db_stage_delete_field_defs` |
| `db_delete_cards` | `db_stage_delete_cards` |
| `db_delete_deck_practice_configs` | `db_stage_delete_deck_practice_configs` |
| `db_delete_practice_runs` | `db_stage_delete_practice_runs` |

Signatures, bodies, and docstrings are unchanged by the rename except where a docstring names one of the 17.

### New `database_ops` functions (ADR 055)

Every function below is a plain module-level function taking `db: Session` first. "Staged" means added and flushed inside the caller's open transaction, never committed; `IntegrityError` from the flush propagates to the caller.

`app/database_ops/app_user.py` (new):
- `db_read_app_user_by_clerk_id(db, clerk_user_id: str) -> AppUser | None`
- `db_create_app_user(db, clerk_user_id: str, timezone: str) -> AppUser` — add, commit, refresh; `IntegrityError` from the commit propagates (the caller rolls back and re-reads, as `dependencies.py` does today).
- `db_update_app_user_timezone(db, user: AppUser, timezone: str) -> AppUser` — sets `user.timezone`, commits, refreshes.

`app/database_ops/deck.py`:
- `db_stage_create_deck(db, subject_id: uuid.UUID, name: str) -> Deck`

`app/database_ops/field_def.py`:
- `db_stage_create_field_def(db, deck_id: uuid.UUID, name: str, field_type: FieldType, position: int) -> FieldDef`. `db_create_field_def` is unchanged.

`app/database_ops/card.py`:
- `db_stage_create_card_field_values(db, card_id: uuid.UUID, values: dict[uuid.UUID, str]) -> None` — one `CardFieldValue` add per item, no flush.
- `db_stage_create_card(db, deck_id: uuid.UUID, values: dict[uuid.UUID, str]) -> Card` — `Card` add and flush, then `db_stage_create_card_field_values`; the returned `Card` does not have `values` loaded.
- `db_read_card_for_deck(db, card_id: uuid.UUID, deck_id: uuid.UUID) -> Card | None` — `Card.id == card_id and Card.deck_id == deck_id`, `values` loaded with `selectinload`, no ownership join (the caller's deck is already ownership-checked).
- `db_create_card` becomes `db_stage_create_card` followed by `db.commit()` and the existing `_read_card_with_values`; behaviour identical.

`app/database_ops/deck_practice_config.py`:
- `db_stage_create_deck_practice_config(db, data: dict) -> DeckPracticeConfig`. `db_create_deck_practice_config` is unchanged.

`app/database_ops/practice_deck.py`:
- `db_stage_unlink_practice_decks_from_config(db, config_id: uuid.UUID) -> None` — the `UPDATE practice_deck SET source_config_id = NULL WHERE source_config_id = :config_id` that `deck_practice_config.py:109-113` issues today, no commit.

`app/database_ops/practice_card.py`:
- `POSITION_CONSTRAINT = "uq_practice_card_practice_run_id"` module constant (replaces `_POSITION_CONSTRAINT` in `practice_run.py`).
- `db_try_stage_create_practice_card(db, data: dict) -> PracticeCard | None` — inside `with db.begin_nested():` executes `SET CONSTRAINTS {POSITION_CONSTRAINT} IMMEDIATE`, adds `PracticeCard(**data)`, flushes; on `IntegrityError` the savepoint is rolled back by the context manager, the function executes `SET CONSTRAINTS {POSITION_CONSTRAINT} DEFERRED` and returns `None`; on success returns the row and leaves the constraint IMMEDIATE, exactly as `practice_run.py:852-866` (as of task 015 T1; `:828-843` at `700b3c8`) behaves today.
- `db_read_run_ids_for_practice_cards(db, practice_card_ids: Collection[uuid.UUID]) -> dict[uuid.UUID, uuid.UUID | None]` — `{practice_card.id: practice_run_id}` for the ids that exist; empty input returns `{}`.

`app/database_ops/mastery_log.py`:
- `db_lock_card(db, card_id: uuid.UUID) -> None` — `db.execute(text("SELECT pg_advisory_xact_lock(hashtext(:key))"), {"key": str(card_id)})`, the statement `mastery.py:55` and `:113` issue today. Carries no prefix (ADR 055). The statement contains no mastery arithmetic, so `test_no_mastery_arithmetic_outside_strategy`'s scan of this file is unaffected.

### Service signatures that change (ADR 055)

- `delete_deck(db, strategy, user_id: uuid.UUID, deck: Deck) -> None` and `delete_card(db, strategy, user_id: uuid.UUID, card: Card) -> None` in `app/services/deletion.py`; `delete_subject` is unchanged. The parent lookups become `db_read_subject(db, deck.subject_id, user_id)` and `db_read_deck(db, card.deck_id, user_id)`, and `compute_deletion_impact` receives `user_id`. Callers: `app/routers/api/deck.py:110`, `app/routers/api/card.py:134`.
- `touch(db, *rows)` in `app/services/activity.py` keeps its signature and stops calling `db.add`; every caller passes rows loaded in the same session, so attribute assignment alone is tracked.

### Guards (ADR 055, MD-1)

`tests/api_tests/test_schema_guard.py` gains:
- `test_alembic_env_imports_app_database_before_reading_metadata` — `alembic/env.py` parsed with `ast` has an `ast.Import` whose alias name is `app.database` and an `ast.Assign` whose single target is `ast.Name(id="target_metadata")`, and the import's `lineno` is less than the assign's; a commented-out import is not an `ast.Import`, so it fails.
- `test_metadata_naming_convention_is_the_adr_054_templates` — after `import app.database`, `SQLModel.metadata.naming_convention == {"ix": "ix_%(column_0_label)s", "uq": "uq_%(table_name)s_%(column_0_name)s", "ck": "ck_%(table_name)s_%(constraint_name)s", "fk": "fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s", "pk": "pk_%(table_name)s"}`.

`tests/api_tests/test_layering_guard.py` (new), four tests, each failure message naming `path:line`:
- `test_only_database_ops_imports_statement_builders` — over every `app/**/*.py` except `app/database_ops/**`, `app/models/**`, and `app/database.py`, parsed with `ast`: an `Import` or `ImportFrom` whose module starts with `sqlmodel` or `sqlalchemy` is allowed only as `from sqlmodel import Session` or an import from `sqlalchemy.exc`.
- `test_only_database_ops_calls_the_session` — over every `app/**/*.py` except `app/database_ops/**`, parsed with `ast`: every `ast.Call` whose `func` is an `ast.Attribute` on `ast.Name(id="db")` has `attr` in `{"commit", "rollback", "flush", "refresh"}`; comments and docstrings are never matched.
- `test_layers_depend_downward_only` — no import under `app/database_ops/` whose module starts with `app.services` or `app.routers`; no import under `app/services/` whose module starts with `app.routers`.
- `test_stage_prefix_matches_commit_behaviour` — for every top-level `def db_*` in `app/database_ops/*.py`: tokens are the name after `db_` split on `_`, a leading `try` is dropped, `staged` is whether the next token is `stage` (then dropped), `verb` is the next token; `commits` is whether the function's body contains an `ast.Call` whose `func` is an `ast.Attribute` with `attr == "commit"` on `ast.Name(id="db")`; if `verb` is in `{"read", "fetch", "count", "next", "lock"}` the function is neither staged nor commits; otherwise `commits` equals `not staged`.

## Tasks

T4 depends on T1 (both edit `practice_run.py` and `test_practice_run.py`). T6, T7, T8 depend on T4 and are independent of one another (disjoint files). T9 depends on T8. T10 depends on T5, T6, T7, T9. T2, T3, and T5 have no dependencies and touch no file any other task touches.

### T1 — Start and rerun refuse an empty practice (ADR 056) — no dependencies

- [x] **Goal:** a practice whose generation writes no cards is refused with `no_cards` on start and on rerun, and nothing is persisted.
- **Files:** `app/services/practice_run.py`, `app/routers/api/practice_run.py` (rerun docstring), `tests/api_tests/test_practice_run.py`, `docs/tasks/004-practice-setup.md`.
- **Details:** Per the error-codes contract: in `start_practice_run`, after the `for config in configs` loop and before `db.commit()`, `if next_position == 0: raise RunStartError("no_cards", "generation produced no practice cards across the selected decks")`; in `rerun_practice_run`, after its `for deck_id, ... in surviving_decks` loop and before `db.commit()`, `if next_position == 0: raise RerunError("no_cards", "generation produced no practice cards across the surviving decks")`. Add the code to the `RunStartError` docstring, the `RerunError` docstring, `start_practice_run`'s and `rerun_practice_run`'s docstrings (each lists the codes it raises), and `rerun_run`'s route docstring. New class `TestNoCards` after `TestRunStartErrorShape`, four tests: (1) `test_start_refuses_a_deck_with_no_cards`: `existing_field_defs` (front, back); `POST /api/deck_practice_configs` with `prompt_field_ids=[front]`, `answer_field_ids=[back]`, empty pools; `POST /api/practice_runs` with that config returns 400 and `detail == {"code": "no_cards", "message": "generation produced no practice cards across the selected decks", "config_id": None}`; `GET /api/practice_runs` returns `[]`. (2) `test_start_succeeds_when_any_selected_deck_has_cards`: `session_cards`, `session_config`; a second deck through `POST /api/decks` with `{"name": "Empty deck", "subject_id": existing_subject["id"], "field_defs": [{"name": "q", "type": "text"}, {"name": "a", "type": "text"}]}` and a configuration on it with `q` as prompt and `a` as answer; `POST /api/practice_runs` with both configuration ids returns 201; `GET /api/practice_runs/{id}/state` has `progress.total_cards == 3`: `session_config` has a fixed prompt and answer plus a pool on each side, and every value of the three `session_cards` is filled, so all three cards resolve (verified on 2026-09-23 with a scratch run). (3) `test_start_refuses_when_every_card_is_blank_on_one_side`: `existing_field_defs`; `POST /api/cards` with `{"deck_id": ..., "values": {front: "a", back: ""}}` returns 201; a configuration with `front` as prompt and `back` as answer; start returns 400 with `detail["code"] == "no_cards"`. (4) `test_rerun_refuses_when_no_card_generates`: `run = _start(client, "Run", [session_config["id"]])`, `_finish_session(client, run["id"])`, `DELETE /api/cards/{id}` for every id in `session_cards` (each returns 204), then `POST /api/practice_runs/{run_id}/rerun` with `{"name": "Again"}` returns 400 and `detail == {"code": "no_cards", "message": "generation produced no practice cards across the surviving decks"}`; `GET /api/practice_runs/{run_id}` still returns 200. In `docs/tasks/004-practice-setup.md`, append to the Superseded bullet that begins "Carried invariant 2's" the sentence: `Enforced since task 015 T1 (ADR 056): start and rerun refuse with \`no_cards\`.`
- **Out of scope:** any frontend file; `_snapshot_and_generate_deck`; the configuration picker; any other docstring.
- **Done when:** `uv run pytest tests/api_tests/test_practice_run.py -q` passes with the four new tests; `grep -n "no_cards" app/services/practice_run.py` shows two raise sites and four docstrings; `uv run pytest` passes in full; the commit touches only the four files above.
- Notes: none. Built in two passes: the first left `start_practice_run`'s and `rerun_practice_run`'s own docstrings alone, because the original Details named only the two exception classes and the route while Out of scope reserved "any other docstring"; the amended Details now name all four, and both were added. The four tests live in `TestNoCards`, inserted after `TestRunStartErrorShape` as specified; it carries a small `_config` helper for the repeated two-field, no-pool configuration POST. 310 backend tests pass.

### T2 — The create page and rerun render the no-cards sentence (MD-2) — no dependencies

- [x] **Goal:** a `no_cards` failure shows `NO_CARDS_MESSAGE` inline on the create page and after a rerun, with the selection intact.
- **Files:** `frontend/src/lib/practiceCopy.ts` (new), `frontend/src/pages/PracticeCreatePage.tsx`, `frontend/src/pages/PracticeCreatePage.test.tsx`, `frontend/src/pages/PracticeDetailsPage.tsx`, `frontend/src/pages/PracticeDetailsPage.test.tsx`.
- **Details:** Per the frontend-copy contract. Create page `onError`: a third `ApiDetailError` branch before the fallback, `if (error.detail.code === 'no_cards') { setTopError(NO_CARDS_MESSAGE); return; }`; the comment above the fallback (`// duplicate_deck (unreachable ...) and anything else.`) is unchanged. Details page: import `ApiDetailError` from `src/api/unwrap`; rerun `onError` becomes `setConfirmRerunOpen(false); setRerunError(error instanceof ApiDetailError && error.detail.code === 'no_cards' ? NO_CARDS_MESSAGE : error.message);`. Tests: in `PracticeCreatePage.test.tsx`, `no_cards shows the no-cards sentence above the list and keeps the selection`, modelled on the `stale_config` test at line 229: the mocked `POST` returns 400 with `{ detail: { code: 'no_cards', message: 'generation produced no practice cards across the selected decks', config_id: null } }`; after clicking Create, `await screen.findByText(NO_CARDS_MESSAGE)` is in the document and the `Recall` radio is still checked. In `PracticeDetailsPage.test.tsx`, `a no_cards rerun failure closes the dialog and shows the no-cards sentence`, modelled on the `nothing_to_rerun` test at line 304, with `{ detail: { code: 'no_cards', message: 'generation produced no practice cards across the surviving decks' } }`; the alert's text content equals `NO_CARDS_MESSAGE` and no dialog is present. No `npm run gen:api`: the error detail is an untyped `HTTPException` body.
- **Out of scope:** the bottom `saveError` slot; any dialog; disabling configurations in the picker; the run page; any backend file.
- **Done when:** in `frontend/`, `npx vitest run`, `npm run lint`, and `npm run build` are clean; `grep -rn "no_cards" frontend/src` lists exactly the two pages and the two tests; `grep -rln "This practice would have no cards" frontend/src` lists only `src/lib/practiceCopy.ts`.
- Notes: none. `practiceCopy.ts`'s header comment is worded to avoid the literal token `no_cards`, so the Done-when grep still lists exactly the two pages and the two tests. The details-page test asserts `textContent` equality rather than `toHaveTextContent`, which is a substring match, so the backend's own message could not pass it. 419 frontend tests pass; lint and build clean.

### T3 — Schema guards for the naming convention (MD-1) — no dependencies

- [x] **Goal:** a dropped `app.database` import in `alembic/env.py`, or a changed naming template, fails the suite.
- **Files:** `tests/api_tests/test_schema_guard.py`.
- **Details:** The two tests per the Guards contract, appended after `test_no_create_all_or_drop_all_under_app`, each with a docstring citing ADR 054. The first parses `alembic/env.py` (`Path(__file__).resolve().parents[2] / "alembic" / "env.py"`) with `ast` and asserts the import node exists, the assign node exists, and the import's line number is the smaller, with messages naming the file; the second does `import app.database` inside the test body and compares `SQLModel.metadata.naming_convention` to the literal dict.
- **Out of scope:** any change to `alembic/env.py` or `app/database.py`; any other test file.
- **Done when:** `uv run pytest tests/api_tests/test_schema_guard.py -q` reports 3 passed; with the `import app.database` line of `alembic/env.py` temporarily commented out, the same command reports the first new test failing, and the line is restored before the commit (`git diff alembic/` is empty).
- Notes: none. The five templates are a module-level `NAMING_CONVENTION` constant in the test file rather than a dict literal inline in the assertion, so the failure message stays readable. The negative check ran as specified and `git diff alembic/` is empty. Worth knowing for T10: the second guard's `from sqlmodel import SQLModel` sits inside the test body, but no planned guard scans `tests/`, so it is not at risk.

### T4 — The 17 non-committing writers gain the `stage_` prefix (ADR 055) — after T1

- [x] **Goal:** every `database_ops` writer that does not commit is named `db_stage_*`, and nothing references an old name.
- **Files:** `app/database_ops/card.py`, `deck.py`, `deck_practice_config.py`, `field_def.py`, `mastery_log.py`, `practice_card.py`, `practice_deck.py`, `practice_run.py`, `review_log.py`, `subject.py`; every importer under `app/services/` and `app/routers/api/`; `tests/api_tests/test_mastery.py`, `test_review_log.py`, `test_practice_run.py`.
- **Details:** The 17 renames in the Naming contract, definitions and every reference, by whole-word replacement (`grep -rlw <old> app/ tests/` then `sed -i 's/\b<old>\b/<new>/g'`), in any order (the `\b` boundaries stop `db_clear_mastery` matching inside `db_clear_mastery_for_deck` and `db_delete_decks` inside `db_delete_deck_practice_configs`), including mentions inside docstrings and comments in `app/` and `tests/` (for example `practice_deck.py:9` "see db_create_practice_run" and `test_practice_run.py:598`). No signature, body, or import order changes. Docs are not edited; the intro of this file lists the task files the next sync reconciles.
- **Out of scope:** any new function; any file under `docs/`; any `db_*` function not in the table.
- **Done when:** `grep -rwn -e db_create_practice_run -e db_create_practice_deck -e db_create_practice_card -e db_insert_review_logs -e db_log_review_group -e db_append_mastery_log -e db_update_practice_card_status -e db_renumber_pending_practice_cards -e db_scrub_shown_prompt_ids -e db_clear_mastery -e db_clear_mastery_for_deck -e db_delete_subjects -e db_delete_decks -e db_delete_field_defs -e db_delete_cards -e db_delete_deck_practice_configs -e db_delete_practice_runs app/ tests/` returns nothing; `uv run pytest` passes in full; `git diff --stat` touches only `app/` and `tests/`.
- Notes: one file outside the Files line was renamed: `app/models/practice_card.py:19` carries the name in a comment, and both the Details ("mentions inside docstrings and comments in `app/` and `tests/`") and the Done-when grep, which scans all of `app/`, require it. Reference counts per name are identical before and after, so nothing was lost or duplicated; all 17 were confirmed non-committing before the rename. Two consequences for later tasks: the renamed names no longer sort alphabetically inside their `from ... import (...)` blocks, left alone because the Details forbid import-order changes, and a handful of lines now run to 101 characters, within the 109 already present at HEAD and ungated (CI lints the frontend only). 312 backend tests pass, unchanged from before the rename.

### T5 — `app_user` gets its own module (ADR 055) — no dependencies

- [x] **Goal:** `app/dependencies.py` reads, creates, and updates the user through `app/database_ops/app_user.py` and issues no statement of its own.
- **Files:** `app/database_ops/app_user.py` (new), `app/dependencies.py`.
- **Details:** The three functions per the contract, with a module docstring naming `app_user` as the table. `_sync_timezone` becomes: compute `zone`; return `user` when `zone is None or zone == user.timezone`; otherwise return `db_update_app_user_timezone(db, user, zone)`. `get_current_app_user`: `user = db_read_app_user_by_clerk_id(db, clerk_user_id)`; if found, sync and return; otherwise `try: user = db_create_app_user(db, clerk_user_id, normalize_timezone(x_timezone) or DEFAULT_TIMEZONE) except IntegrityError: db.rollback(); user = db_read_app_user_by_clerk_id(db, clerk_user_id); assert user is not None, "IntegrityError on clerk_user_id implies a row exists"; return _sync_timezone(db, user, x_timezone)`, then `return user`. The concurrent-first-sight comment stays on the `except`. Remove `from sqlmodel import select`; keep `from sqlalchemy.exc import IntegrityError`.
- **Out of scope:** the dev-auth bypass and its `TODO(defer:dev-auth-bypass)`; `app/verify_clerk_session.py`; `app/services/timezone.py`; any test change (the existing tests pin the behaviour).
- **Done when:** `uv run pytest tests/api_tests/test_user_timezone.py tests/api_tests/test_auth_scoping.py tests/api_tests/test_verify_clerk_session.py -q` passes unchanged; `grep -n "select\|db\.exec\|db\.add\|db\.get" app/dependencies.py` returns nothing; `grep -c "^def db_" app/database_ops/app_user.py` prints 3.
- Notes: none. `db_update_app_user_timezone` drops the `db.add(user)` the old `_sync_timezone` had, as the contract's wording ("sets `user.timezone`, commits, refreshes") specifies and for the same reason T6 gives for `touch`: the row was loaded by this session, so the assignment is already tracked. Pre-checked against T10's four rules with an `ast` scan: `app/dependencies.py` no longer appears under the statement-import or session-call rules, and all three new functions satisfy the `stage_` rule (one `read` that does not commit, two unprefixed writers that do). 36 pinned tests and 312 backend tests pass.

### T6 — Routers, deletion, `touch`, and the subject update stop touching the session (ADR 055) — after T4

- [x] **Goal:** no router or deletion function calls `db.get`, `touch` makes no session call, and `database_ops/subject.py` imports nothing from `app.services`.
- **Files:** `app/routers/api/field_def.py`, `app/routers/api/card.py`, `app/routers/api/deck.py`, `app/routers/api/subject.py`, `app/services/deletion.py`, `app/services/activity.py`, `app/database_ops/subject.py`.
- **Details:** the `db.get(Deck, field_def.deck_id)` calls in `update_field_def`, `archive_field_def`, and `hard_delete_field_def` (`field_def.py`) and the `db.get(Deck, card.deck_id)` call in `update_card` (`card.py`) become `db_read_deck(db, <that id>, current_user.id)` (already imported in both routers); delete `from app.models.deck import Deck` in both files, whose only uses those lines were. `deletion.py`: the signatures per the service-signatures contract, the two `db.get` calls replaced as stated, `compute_deletion_impact(db, user_id, ...)` in `delete_deck` and `delete_card`, the two `assert ... is not None` lines kept; the `delete_deck_service(...)` call in `deck.py` and the `delete_card_service(...)` call in `card.py` pass `current_user.id` as the third argument. `activity.py`: delete `db.add(row)`; the docstring gains one sentence: rows must be loaded in this session, since attribute assignment alone marks them dirty. `database_ops/subject.py`: delete `from app.services.activity import touch` and the `touch(db, subject)` line plus its `# D13` comment in `db_update_subject`. `routers/api/subject.py` `update_subject`: `data = payload.model_dump(exclude_unset=True)`; `if data: touch(db, subject)` before `db_update_subject(db, subject, data)`, importing `touch` from `app.services.activity`; an empty PATCH therefore still does not bump `last_activity_at`, as today.
- **Out of scope:** `compute_deletion_impact` and `apply_deletion` bodies; the dormant archive and hard-delete routes beyond their `db.get` lines; any test change.
- **Done when:** `uv run pytest` passes in full (`test_activity_tracking.py` and `test_deletion.py` pin the behaviour); `grep -rn "db\.get(" app/routers app/services/deletion.py app/services/activity.py` returns nothing; `grep -n "app.services" app/database_ops/subject.py` returns nothing; `grep -n "db\." app/services/activity.py` returns nothing.
- Notes: none. First built with the Done-when's `db.get(` grep spanning all of `app/services`, which also caught two sites in `deck_batch_edit.py` that T9 owns; a planning session scoped the grep to T6's own files and ticked the box, and the amended criterion was re-verified before T7 started. `delete_card` lost its subject lookup entirely rather than converting it: it existed only to supply `user_id`, which is now a parameter, so the contract's two named lookups and two kept asserts are what remain. An `ast` scan confirms `database_ops` has zero upward imports. 312 backend tests pass, unchanged.

### T7 — The practice path: lock, savepoint, attribution, config validation and sever (ADR 055) — after T4

- [x] **Goal:** `mastery.py`, `practice_run.py`, and `deck_practice_config.py` construct no statement and call no session method outside the allowlist.
- **Files:** `app/services/mastery.py`, `app/services/practice_run.py`, `app/services/deck_practice_config.py`, `app/database_ops/mastery_log.py`, `app/database_ops/practice_card.py`, `app/database_ops/practice_deck.py`.
- **Details:** New functions per the contract. `mastery.py`: `apply_rating` and `record_review_group` call `db_lock_card(db, group.card_id)` in place of their `db.execute(text(...))`; `deck_mastery` uses `card_ids = list(db_read_card_ids_for_decks(db, deck_ids))`; `_fetch_run_attribution` returns `db_read_run_ids_for_practice_cards(db, review_group_ids)`; remove the `text` (sqlalchemy) and `col`, `select` (sqlmodel) imports, whose only uses were these sites; drop the `Card` and `PracticeCard` model imports if, after the edit, `grep -n "\bCard\b" app/services/mastery.py` (respectively `"\bPracticeCard\b"`) matches only the import line. `practice_run.py`: delete `_POSITION_CONSTRAINT`, the `text` import, and the `IntegrityError` import (its only `except` moves into `database_ops`); the `for _attempt in range(2)` loop in `_requeue_failed_card` becomes, per attempt, the existing `pending`/`scores`/`position` computation, then `new_card = db_try_stage_create_practice_card(db, {...same dict...})`, `if new_card is not None: return new_card`, then `db_stage_renumber_pending_practice_cards(db, old_card.practice_run_id)`; the `RuntimeError` after the loop and the comment block above it stay (the comment's "inside a savepoint" now describes the `database_ops` function). `deck_practice_config.py`: `validate_deck_practice_config` computes `live_ids = all_ids & db_read_active_field_ids_for_decks(db, [deck_id])`; `update_deck_practice_config`'s unlink becomes `db_stage_unlink_practice_decks_from_config(db, config.id)`; the import line `from sqlmodel import Session, col, select, update` becomes `from sqlmodel import Session`, and the `FieldDef` and `PracticeDeck` model imports are deleted (their only uses were these two sites).
- **Out of scope:** `_insertion_position`, `RETRY_SPACING_FLOOR`, `_POSITION_GAP`; `submit_rating`'s harshest-wins rule; any `MasteryStrategy`; any test change (`TestPositionCollisionFallback`, `TestRetrySpacingFloor`, `TestConfigEditSeversLineage`, and `test_mastery.py` pin the behaviour).
- **Done when:** `uv run pytest` passes in full; `grep -n "text(\|select(\|update(\|db\.execute\|db\.exec\|begin_nested" app/services/mastery.py app/services/practice_run.py app/services/deck_practice_config.py` returns nothing; `uv run pytest tests/api_tests/test_mastery.py -q -k no_mastery_arithmetic` passes.
- Notes: none. Three HOW choices, none changing behaviour. `db_try_stage_create_practice_card` calls the existing `db_stage_create_practice_card` inside its savepoint rather than repeating the add and flush, which is what the inline code did too; returning from inside the `with` releases the savepoint on exit exactly as the old assign-then-return did. `validate_deck_practice_config` now reads every active field of the deck and intersects in Python rather than filtering by id in SQL, per the Details; the resulting set is identical. `deck_mastery`'s `card_ids` is now a `list` of a `set`, so its order is no longer the query's; harmless, because the rows come from `db_fetch_mastery_read_rows` and are grouped into dicts. `TestPositionCollisionFallback` asserts the occupied card moved off position 0, which only happens when the new function returns `None`, so the moved savepoint's collision branch is genuinely exercised. Both `Card` and `PracticeCard` imports were dropped from `mastery.py` under the Details' grep test. An `ast` pre-check of T10's four rules finds all three T7 services clean and every new function correctly named. 312 backend tests pass.

### T8 — Deck create and copy build rows through staged functions (ADR 055) — after T4

- [x] **Goal:** `deck_create.py` and `deck_copy.py` call no `db.add`, and `db_create_card` is built on the staged create.
- **Files:** `app/services/deck_create.py`, `app/services/deck_copy.py`, `app/database_ops/deck.py`, `app/database_ops/field_def.py`, `app/database_ops/card.py`, `app/database_ops/deck_practice_config.py`.
- **Details:** New functions per the contract, and `db_create_card` rewritten as stated. `deck_create.py`: `deck = db_stage_create_deck(db, subject_id, name)` inside the existing `try`, with the `except IntegrityError: db.rollback(); raise ...` unchanged; the field loop calls `db_stage_create_field_def(db, deck.id, field_name, field_def.type, position)`; the `db.flush()` after the loop stays; delete the `FieldDef` model import (its only use was the constructor); `Deck` stays for the return annotation. `deck_copy.py`: `new_deck = db_stage_create_deck(...)` inside its existing `try`; each field through `db_stage_create_field_def(db, new_deck.id, field_def.name, field_def.type, field_def.position)`; each card as `values = {field_map[v.field_def_id]: v.value for v in card.values if v.field_def_id in field_map}` then `db_stage_create_card(db, new_deck.id, values)` (the archived-field skip is the `in field_map` test, keep its comment); each config through `db_stage_create_deck_practice_config(db, {...})` inside its existing `try`; delete the `Card`, `CardFieldValue`, `FieldDef`, and `DeckPracticeConfig` model imports (constructors were their only uses); `Deck` stays for the return annotation.
- **Out of scope:** validation rules in either service; the copy endpoint's exposure (ADR 014); `deck_batch_edit.py` (T9); any test change (`test_decks.py`, `test_cards.py`, `test_deck_copy.py` pin the behaviour).
- **Done when:** `uv run pytest` passes in full; `grep -n "db\.add\|db\.exec\|db\.get\|select(" app/services/deck_create.py app/services/deck_copy.py` returns nothing; `grep -c "db.commit()" app/database_ops/card.py` is unchanged from before the task.
- Notes: none. `db_read_card_for_deck`, also listed under the card module in the Contracts, was left for T9, whose Details claim it by name; T8 added only the four create functions. The staged creates carry no `IntegrityError` handler, per the contract's "propagates to the caller", so each caller's existing handler still produces its own message. COVERAGE GAP for a planning session: the Out of scope says the three test files pin this behaviour, but only one of the three duplicate-name handlers is pinned (`test_deck_name_collision_on_target_subject_rejected`, copy's deck name). Create's `already exists in this subject` 422 and copy's `already exists on the new deck` `ValueError` are asserted nowhere. Both were verified with a throwaway test, deleted before this report: the second POST of a same-named deck returns 422 with that message and leaves one deck; copying one configuration twice raises that `ValueError` and leaves the target subject empty. Worth two permanent tests in a later task.

### T9 — The batch edit reads and writes through `database_ops` (ADR 055) — after T8

- [x] **Goal:** `deck_batch_edit.py` constructs no statement and calls only `flush`, `commit`, `rollback`, and `refresh` on the session.
- **Files:** `app/services/deck_batch_edit.py`, `app/database_ops/card.py`.
- **Details:** `db_read_card_for_deck` per the contract. Site by site (line numbers as of commit `700b3c8`; no earlier task edits this file): the `existing_card_ids` snapshot (`:72-74`) becomes `existing_card_ids = db_read_card_ids_for_deck(db, deck.id)`; the `active_fields` dict (`:76-83`) becomes `active_fields = {fd.id: fd for fd in db_read_field_defs(db, deck.id, user_id)}`; in the `field_defs.create` loop, `row = FieldDef(...)`, `db.add(row)`, `db.flush()` (`:101-108`) become `row = db_stage_create_field_def(db, deck.id, field_name, entry.type, db_next_position(db, deck.id))` (the staged function flushes); the D10 backfill loop (`:114-115`) becomes `for card_id in existing_card_ids: db_stage_create_card_field_values(db, card_id, {row.id: ""})`; the `db.add(field)` after the `field_defs.update` loop (`:134`) and after the reorder loop (`:174`) are deleted; the two `db.exec(select(Card)...)` lookups in the `cards.delete` and `cards.update` loops (`:185-187`, `:203-205`) become `card = db_read_card_for_deck(db, card_id_or_entry_id, deck.id)`; in the `cards.update` values loop (`:220-231`) compute `existing = {v.field_def_id: v for v in card.values}` once per card before its values loop, then `if field_id in existing: existing[field_id].value = stored_value else: db_stage_create_card_field_values(db, card.id, {field_id: stored_value})` (the `db.flush()` at `:176` precedes every card read, so a row backfilled for a field created in this request is already in `card.values`); the `cards.create` loop's `Card(...)` add, flush, and `CardFieldValue(...)` adds (`:244-254`) become `db_stage_create_card(db, deck.id, {field_id: resolved_values.get(field_id, "") for field_id in active_fields})`; the `subjects = [db.get(Subject, sid) ...]` line (`:259`) becomes `subjects = [db_read_subject(db, sid, user_id) for sid in touch_subject_ids]`; the `db.add(deck)` before the commit (`:262`) is deleted. Delete the `Card`, `CardFieldValue`, and `Subject` model imports and reduce `from sqlmodel import Session, col, select` to `from sqlmodel import Session`; `FieldDef` stays for the type hints. The `try: db.commit() except IntegrityError: db.rollback()` at the end is unchanged.
- **Out of scope:** the phase order; any validation message; MD-1 of task 013 (type change refusal); the deletion calls; any test change (`test_deck_batch_edit.py` pins the behaviour).
- **Done when:** `uv run pytest` passes in full; `grep -n "db\.add\|db\.exec\|db\.get\|select(" app/services/deck_batch_edit.py` returns nothing; `grep -n "^from sqlmodel import" app/services/deck_batch_edit.py` prints exactly `from sqlmodel import Session`.
- Notes: none. The cited line numbers held: the file was unchanged since `700b3c8`. The one site with real risk is the `cards.update` values loop, where a primary-key `db.get`, which consults the identity map, became a dict over `card.values`, which does not refresh a collection already loaded. The Details' justification was verified rather than assumed: the route loads only the deck, `Deck` has no relationships, and neither the deletion closure nor the mastery rebuild loads a `Card` object, so each card's first load is the update loop's own read, after the field phase's flush. A temporary mutation forcing the create branch failed `test_field_create_and_existing_card_update_via_client_key` and two others, so the dependency is pinned; the precondition is also written into `db_read_card_for_deck`'s docstring. One divergence from `db.get` survives, unreachable on dense data: two update entries for the same card, the first hitting the create branch, would now add a duplicate row; the create branch runs only when the density invariant is already broken. `active_fields` now arrives ordered by position through an ownership join; the set is the same, and the join still holds when the same edit moves the deck, since the target subject is validated first. An `ast` pre-check of all four T10 rules over `app/` is clean. 312 backend tests pass.

### T10 — The layering guard (ADR 055) — after T5, T6, T7, T9

- [ ] **Goal:** a statement built outside `database_ops`, a session call outside the allowlist, an upward import, or a misnamed writer fails the suite.
- **Files:** `tests/api_tests/test_layering_guard.py` (new).
- **Details:** The four tests per the Guards contract, in the style of `test_models_layout_guard.py` (`ast` for imports, `Path(__file__).resolve().parents[2]` for the root, `sorted(rglob("*.py"))`, one assertion message per offending `path:line`). The staged test derives verb and prefix from the function name exactly as the contract states and walks each function's body with `ast.walk` for the commit call; no test reads source text with a regex.
- **Out of scope:** scanning `tests/` or `alembic/`; any change to a file under `app/` (if a test fails on a site an earlier task missed, mark this task `[BLOCKED]` with the offending `path:line` as the brief rather than editing it here).
- **Done when:** `uv run pytest tests/api_tests/test_layering_guard.py -q` reports 4 passed; three temporary mutations each make exactly one test fail and are reverted before the commit (`git diff app/` is empty): a line `deck = db.get(Deck, deck_id)` added inside the body of `delete_deck` in `app/routers/api/deck.py`, a `from app.services.activity import touch` line added to `app/database_ops/deck.py`, and a `db.commit()` line appended to the body of `db_stage_create_deck` in `app/database_ops/deck.py`; `uv run pytest` passes in full.
- Notes:
