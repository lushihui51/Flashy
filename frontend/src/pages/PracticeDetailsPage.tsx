// TODO(defer:practice-details) stub landing page for a session row clicked in the
// overview, and for the redirect after a session is created. Phase 4 fills it in with
// the session's name, status badge, created date, deck/subject chips, and — for an
// active session — the Start Practice button (pure navigation: create is start, so the
// cards already exist).
export default function PracticeDetailsPage() {
  return (
    <div className="p-4">
      <h1 className="text-xl font-semibold text-(--color-text)">Practice session</h1>
      <p className="text-(--color-text-muted)">Coming soon.</p>
    </div>
  );
}
