// Phase 5.5: one constant, never a literal — every search dropdown (the deck editor's
// subject combobox, the card form's deck combobox) caps its list at this many matches.
export const PICKER_MAX_ITEMS = 8;
